# MY AI Web

Vercel-ready static site with a serverless `/api/chat` function.

Required Vercel environment variables:
- `OPENROUTER_API_KEY`
- `OPENROUTER_MODEL` = `openrouter/free`

Do not upload `.env`.
